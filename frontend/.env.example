# URL pública del backend desplegado en Render, INCLUYENDO el sufijo /api
# En desarrollo local se puede dejar vacío (se usa el proxy de vite.config.js hacia localhost:4000)
# En producción (Vercel) es OBLIGATORIA. Ejemplo:
# VITE_API_URL=https://agroroute-backend.onrender.com/api
VITE_API_URL=
